import React from "react";

const Footer = () => {
  return (
    <footer>
      <h4 style={{ textAlign: "center" }}>
        I am a footer, I contain great magicks (but mostly I am a placeholder so
        that the components above me look better spacially on the page)
      </h4>
    </footer>
  );
};

export default Footer;
